pipeline {
    agent any
    environment {
        // Define environment variables for repositories, credentials, and tools
        GIT_CREDENTIALS_ID = 'devops_pi'
        // FRONT_REPO_URL = 'git@your-private-repo.com:front-end.git'
        BACK_REPO_URL = 'https://github.com/WalidTouj/PiDevArctic.git'
        MAVEN_TOOL = 'M2_HOME'   // Name of the Maven tool configured in Jenkins
        JDK_TOOL = 'JAVA_HOME'           // Name of the JDK tool configured in Jenkins
    }
    tools {
        maven "${MAVEN_TOOL}"
        jdk "${JDK_TOOL}"
    }
    stages {
      
            
                // stage('Clone Front-End') {
                //     steps {
                //         // Pull the front-end repo
                //         git branch: 'main', credentialsId: "${GIT_CREDENTIALS_ID}", url: "${FRONT_REPO_URL}"
                //     }
                // }
                stage('Clone Back-End') {
                    steps {
                        // Pull the back-end repo
                        git branch: 'user', credentialsId: "${GIT_CREDENTIALS_ID}", url: "${BACK_REPO_URL}"
                    }
                }